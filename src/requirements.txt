# Milvus client:
# - Windows: Milvus Lite is NOT supported, so use server URI (e.g. http://localhost:19530)
# - non-Windows (Linux/Render): enable Milvus Lite so we can use a local db file (data/db/milvus.db)
pymilvus[model]; sys_platform == "win32"
pymilvus[model,milvus-lite]; sys_platform != "win32"
neo4j           # graph database
neo4j_graphrag  # graph rag
python-dotenv   # env variables
lmstudio        # local llm client
openai          # openai client
sentence-transformers # local embeddings (free)
pdfminer.six    # pdf
PyPDF2          # pdf
pymupdf4llm     # pdf to markdown
docling         # chunking
markdown-chunker # markdown to chunking
deepdiff        # content differ
groq            # groq client

# Compatibility pin: several common scientific/NLP libs (e.g., gensim) still require NumPy < 2.0.
# This prevents pip from pulling NumPy 2.x and ending up with an inconsistent environment.
numpy==1.26.4

# Required by neo4j_graphrag experimental pipeline (kg_builder)
rapidfuzz

# Required by neo4j_graphrag experimental pipeline resolver
spacy>=3.7.0